{
  "name": "@plinth-pages/shared",
  "version": "0.0.0",
  "private": true,
  "description": "API contract types shared by backend and admin. Types only — import with `import type`.",
  "types": "./src/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.d.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p ."
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
